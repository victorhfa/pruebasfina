<!-- views/edit-profile.ejs -->
<!doctype html>
<html>
<head>
    <title>Node Authentication</title>
    <!-- CSS -->
    <link rel="stylesheet" href="libs/bootstrap/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="libs/font-awesome/css/font-awesome.min.css">
    <style>
        body        { padding-top:80px; word-wrap:break-word; }
    </style>

    <!-- JS -->
    <script src="libs/angular/angular.min.js"></script>
    <script src="libs/angular-route/angular-route.min.js"></script>

    <!-- ANGULAR CUSTOM -->
    <script src="js/app.js"></script>
</head>
<body>
<div class="container" ng-app="authApp" ng-controller="miController">

<div class="col-sm-6 col-sm-offset-3">

    <h1><span class="fa fa-sign-in"></span> Edit Profile</h1>

    <!-- show any messages that come back with authentication -->
    <% if (message.length > 0) { %>
        <div class="alert alert-danger"><%= message %></div>
    <% } %>

    <!-- EDIT PROFILE FORM -->
    <form action="/edit-profile" method="post" name="userForm" ng-submit="" novalidate>
      <div class="form-group" ng-class="{'has-error' : userForm.email.$invalid && !userForm.email.$pristine }">
          <label>Email</label>
          <input type="email" class="form-control" name="email" ng-model="email" required ng-value="">
          <p ng-show="userForm.email.$invalid && !userForm.email.$pristine" class="help-block">Enter a valid email.</p>
      </div>
      <div class="form-group" ng-class="{'has-error' : userForm.password.$invalid && !userForm.password.$pristine }">
          <label>Password</label>
          <input type="password" class="form-control" name="password" ng-model="password"
          ng-minlength="4" ng-maxlength="16" required>
          <p ng-show="userForm.password.$error.minlength" class="help-block">Password is too short.</p>
          <p ng-show="userForm.password.$error.maxlength" class="help-block">Password is too long.</p>
      </div>

      <button type="submit" class="btn btn-success btn-lg" ng-disabled="userForm.$invalid">Upload Changes</button>
    </form>
    <a href="/profile" class="btn btn-warning btn-lg">Cancel</a>

</div>

</div>
</body>
</html>
